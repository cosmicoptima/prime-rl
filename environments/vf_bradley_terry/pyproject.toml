[project]
name = "bradley-terry"
version = "1.0.0"
dependencies = [
    "choix>=0.4.1",
    "numpy",
    "verifiers>=0.1.2.post0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["vf_bradley_terry.py"]
